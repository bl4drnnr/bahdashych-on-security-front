import React from "react";
import { IUsers } from "../../models/response/users.interface";

const AdminUser = ({ users }: { users: IUsers | undefined }) => {
  return (
    <div>

    </div>
  );
};

export default AdminUser;
